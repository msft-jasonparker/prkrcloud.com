name: Upload To Azure Blob Storage (alt)
on: workflow_dispatch
jobs:
  upload:
    runs-on: windows-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
        with:
          submodules: true

      - name: Check Az cli
        run: |
          az login --service-principal -u "prkrcloud-sp" -p ${{ secrets.AZURE_SP_CREDENTALS.clientSecret }} --tenant ${{ secrets.AZURE_SP_CREDENTALS.tenantId }}
          az account show
          az storage -h
